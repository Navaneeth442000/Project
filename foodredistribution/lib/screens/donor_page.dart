import 'package:flutter/material.dart';

class ScreenDonor extends StatelessWidget {
  const ScreenDonor({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome Donor'),
      ),
    );
  }
}