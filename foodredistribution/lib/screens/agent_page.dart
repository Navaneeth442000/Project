import 'package:flutter/material.dart';

class ScreenAgent extends StatelessWidget {
  const ScreenAgent({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Welcome Agent'),
      ),
    );
  }
}